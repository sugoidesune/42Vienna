[poolworkers]

user = www-data
group = www-data

; change to TCP instead of default socket
listen = 7777

; process manager
pm = dynamic

pm.max_children = 5
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3

clear_env = no